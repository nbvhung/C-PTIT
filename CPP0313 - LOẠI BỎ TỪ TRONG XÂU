#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    string s,a;
	getline(cin,s);
	cin >> a;
	while(1){
		int x = s.find(a);
		if (x == -1) break;
		s.erase(x, a.length()+1);
	}
	cout << s;
}
