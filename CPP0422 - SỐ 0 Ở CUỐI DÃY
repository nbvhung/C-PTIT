#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(){
    int t; cin>>t;
    while(t--){
        int n; cin>>n;
        ll a[n];
        vector<ll> v;
        int cnt=0;
        for(int i=0; i<n; i++){
            cin>>a[i];
            if(a[i]!=0) v.push_back(a[i]);
            else cnt++;
        }
        for(ll x : v) cout<<x<<" ";
        for(int i=1; i<=cnt; i++) cout<<0<<" ";
        cout<<endl;
    }
}
